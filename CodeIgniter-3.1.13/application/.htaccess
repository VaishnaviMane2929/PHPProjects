<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /codeigniter-3.1.13/   # 🔁 Change to your project folder name
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
